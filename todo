<API Document>
accounts/　
  GET => 全てのアカウント情報を取得。

accounts/{number}/
  GET => 指定したIDに合致するアカウントの情報をjsonとして取得する。
  DELETE => 指定したIDに合致するアカウントを削除する

accounts/{number}/ + bodyParameter
  POST => 新しいアカウントを作成するための情報をJSONで送信し、登録する。
  PUT => アカウント情報を変更する
  PATCH => 預金残高を変更させる。ボディパラメーターの"trade"の値によって預金・引き出し・送金を切り替える

accounts/{number}/balance
  GET => 指定のIDの預金残高を取得

accounts/balance?max-amount={number}&min-amount={number}
  GET => 指定の預金残高を持っているアカウントの情報を取得
